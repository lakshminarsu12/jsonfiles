private MachineEvent firstServiceBoxEventSkippable(FindDeviceResponse a) {
    if (!isTrue(a.getPowerCableConnected())) {
        return MachineEvent.ONT_TROUBLESHOOT_SERVICEBOX_POWER_TO_BOX;
    }
    if (isTrue(a.getCoverPresent()) && eq(a.getCoverStatus(), "on")) {
        return MachineEvent.ONT_TROUBLESHOOT_SERVICEBOX_REMOVE_COVER;
    }
    if (!isTrue(a.getCoverPresent()) || eq(a.getCoverStatus(), "off")) {
        return MachineEvent.ONT_TROUBLESHOOT_SERVICEBOX_POWER_BUTTON_PRESS;
    }
    return MachineEvent.ONT_TROUBLESHOOT_SERVICEBOX_SCAN;
}


transitions.withExternal().source(ONT_MISSING_CABLE)
    .target(ONT_TROUBLESHOOT_SERVICEBOX_POWER_TO_BOX)
    .event(ONT_TROUBLESHOOT_SERVICEBOX_POWER_TO_BOX);

transitions.withExternal().source(ONT_MISSING_CABLE)
    .target(ONT_TROUBLESHOOT_SERVICEBOX_POWER_TO_WALL)
    .event(ONT_TROUBLESHOOT_SERVICEBOX_POWER_TO_WALL);

transitions.withExternal().source(ONT_MISSING_CABLE)
    .target(ONT_TROUBLESHOOT_SERVICEBOX_REMOVE_COVER)
    .event(ONT_TROUBLESHOOT_SERVICEBOX_REMOVE_COVER);

transitions.withExternal().source(ONT_MISSING_CABLE)
    .target(ONT_TROUBLESHOOT_SERVICEBOX_POWER_BUTTON_PRESS)
    .event(ONT_TROUBLESHOOT_SERVICEBOX_POWER_BUTTON_PRESS);

transitions.withExternal().source(ONT_MISSING_CABLE)
    .target(ONT_TROUBLESHOOT_SERVICEBOX_SCAN)
    .event(ONT_TROUBLESHOOT_SERVICEBOX_SCAN);

// Linear next-steps
transitions.withExternal().source(ONT_TROUBLESHOOT_SERVICEBOX_POWER_TO_BOX)
    .target(ONT_TROUBLESHOOT_SERVICEBOX_POWER_TO_WALL)
    .event(ONT_TROUBLESHOOT_SERVICEBOX_POWER_TO_WALL);

transitions.withExternal().source(ONT_TROUBLESHOOT_SERVICEBOX_POWER_TO_WALL)
    .target(ONT_TROUBLESHOOT_SERVICEBOX_REMOVE_COVER)
    .event(ONT_TROUBLESHOOT_SERVICEBOX_REMOVE_COVER);

transitions.withExternal().source(ONT_TROUBLESHOOT_SERVICEBOX_REMOVE_COVER)
    .target(ONT_TROUBLESHOOT_SERVICEBOX_POWER_BUTTON_PRESS)
    .event(ONT_TROUBLESHOOT_SERVICEBOX_POWER_BUTTON_PRESS);

transitions.withExternal().source(ONT_TROUBLESHOOT_SERVICEBOX_POWER_BUTTON_PRESS)
    .target(ONT_TROUBLESHOOT_SERVICEBOX_SCAN)
    .event(ONT_TROUBLESHOOT_SERVICEBOX_SCAN);